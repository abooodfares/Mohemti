import 'package:flutter/material.dart';

class NoTaskImg extends StatelessWidget {
  const NoTaskImg({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(child: Image.asset('Assets/imgs/no task .png'));
  }
}